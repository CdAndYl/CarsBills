<UserControl x:Class="CarsBill.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="Cog" Width="64" Height="64" 
                                     HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,15,0,5"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="User, Permission, Backup Management (In Development...)" FontSize="14" Foreground="#757575"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
