<UserControl x:Class="CarsBill.WPF.Views.BaseInfo.CarInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Margin="0,0,10,0"
                     materialDesign:HintAssist.Hint="Search license plate / owner"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     materialDesign:TextFieldAssist.HasClearButton="True"/>
            <Button Content="Add" Margin="5,0" Command="{Binding AddCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
            <Button Content="Refresh" Margin="5,0" Command="{Binding LoadCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" 
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CarId}" Width="60"/>
                <DataGridTextColumn Header="License Plate" Binding="{Binding LicensePlate}" Width="150"/>
                <DataGridTextColumn Header="Owner" Binding="{Binding OwnerName}" Width="120"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="150"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd}'}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Edit Panel -->
        <Border Grid.Row="2" Background="#F5F5F5" CornerRadius="4" Padding="15" Margin="0,10,0,0"
                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Edit Vehicle Info" FontSize="14" FontWeight="Medium" Margin="0,0,0,10"/>
                <WrapPanel>
                    <StackPanel Margin="0,0,20,0">
                        <TextBox Width="180" Margin="0,0,0,8"
                                 materialDesign:HintAssist.Hint="License Plate *"
                                 Text="{Binding EditLicensePlate, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,20,0">
                        <TextBox Width="150" Margin="0,0,0,8"
                                 materialDesign:HintAssist.Hint="Owner Name"
                                 Text="{Binding EditOwnerName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,20,0">
                        <TextBox Width="150" Margin="0,0,0,8"
                                 materialDesign:HintAssist.Hint="Phone Number"
                                 Text="{Binding EditPhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </WrapPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <Button Content="Save" Margin="0,0,10,0" Command="{Binding SaveCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    <Button Content="Delete" Margin="20,0,0,0" Command="{Binding DeleteCommand}"
                            Foreground="Red" BorderBrush="Red"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Visibility="{Binding IsNew, Converter={StaticResource InverseBoolToVisConverter}}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
