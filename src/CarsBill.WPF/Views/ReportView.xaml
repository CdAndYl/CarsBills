<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="CarsBill.WPF.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="280"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource AppCardStyle}" Margin="0,0,0,12">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="FilterCogOutline" Width="20" Height="20" Foreground="#1D4ED8" Margin="0,0,8,0"/>
                    <TextBlock Text="统计筛选" Style="{StaticResource SectionTitleTextStyle}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Command="{Binding ExportCommand}" Style="{StaticResource AppPrimaryButtonStyle}" Content="导出Excel" Margin="0,0,8,0"/>
                    <Button Command="{Binding LoadCommand}" Style="{StaticResource AppSecondaryButtonStyle}" Content="刷新"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                    <StackPanel Width="180" Margin="0,0,10,0">
                        <TextBlock Text="开始日期" Style="{StaticResource FieldCaptionTextStyle}"/>
                        <DatePicker SelectedDate="{Binding FilterStartDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                    </StackPanel>
                    <StackPanel Width="180">
                        <TextBlock Text="结束日期" Style="{StaticResource FieldCaptionTextStyle}"/>
                        <DatePicker SelectedDate="{Binding FilterEndDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                    </StackPanel>
                    <StackPanel Width="280" Margin="10,0,0,0">
                        <TextBlock Text="车牌号（逗号/换行分隔）" Style="{StaticResource FieldCaptionTextStyle}"/>
                        <TextBox Text="{Binding PlateFilterText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:TextFieldAssist.HasClearButton="True"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 MinHeight="56"/>
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,12">
            <Border Style="{StaticResource AppSubtleCardStyle}" Margin="0,0,8,0" Background="#EEF6FF" BorderBrush="#D8EAFE">
                <StackPanel>
                    <materialDesign:PackIcon Kind="FileDocumentMultipleOutline" Width="20" Height="20" Foreground="#1D4ED8"/>
                    <TextBlock Text="总记录数" Style="{StaticResource FieldCaptionTextStyle}" Margin="0,10,0,4"/>
                    <TextBlock Text="{Binding TotalRecords}" FontSize="26" FontWeight="SemiBold" Foreground="#1E3A8A"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource AppSubtleCardStyle}" Margin="4,0" Background="#EDFDF3" BorderBrush="#CFF1DC">
                <StackPanel>
                    <materialDesign:PackIcon Kind="TruckFastOutline" Width="20" Height="20" Foreground="#15803D"/>
                    <TextBlock Text="总车数" Style="{StaticResource FieldCaptionTextStyle}" Margin="0,10,0,4"/>
                    <TextBlock Text="{Binding TotalCarCount}" FontSize="26" FontWeight="SemiBold" Foreground="#166534"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource AppSubtleCardStyle}" Margin="4,0" Background="#FFF7ED" BorderBrush="#FEE3C6">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CurrencyCny" Width="20" Height="20" Foreground="#C2410C"/>
                    <TextBlock Text="总金额" Style="{StaticResource FieldCaptionTextStyle}" Margin="0,10,0,4"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:N2}'}" FontSize="26" FontWeight="SemiBold" Foreground="#9A3412"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource AppSubtleCardStyle}" Margin="8,0,0,0" Background="#F5F3FF" BorderBrush="#E6DDFF">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CarMultiple" Width="20" Height="20" Foreground="#7E22CE"/>
                    <TextBlock Text="车辆数" Style="{StaticResource FieldCaptionTextStyle}" Margin="0,10,0,4"/>
                    <TextBlock Text="{Binding VehicleCount}" FontSize="26" FontWeight="SemiBold" Foreground="#6B21A8"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource AppCardStyle}" Margin="0,0,8,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="每日趋势" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,8"/>
                    <lvc:CartesianChart Series="{Binding DailySeries}" XAxes="{Binding DailyXAxes}"/>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource AppCardStyle}" Margin="8,0,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="车辆分布" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,8"/>
                    <lvc:PieChart Series="{Binding VehicleSeries}"/>
                </DockPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource AppCardStyle}" Margin="0,12,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="车辆汇总明细" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,10"/>
                <DataGrid ItemsSource="{Binding VehicleSummaries}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Style="{StaticResource AppDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="车牌号" Binding="{Binding LicensePlate}" Width="180"/>
                        <DataGridTextColumn Header="趟数" Binding="{Binding TripCount}" Width="90"/>
                        <DataGridTextColumn Header="车数" Binding="{Binding CarCount}" Width="110"/>
                        <DataGridTextColumn Header="总金额" Binding="{Binding TotalAmount, StringFormat='{}{0:N2}'}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>

